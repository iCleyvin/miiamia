# HANDOFF — sesiones 2026-07-01 → 2026-07-02 (Claude)

> Registro de TODO lo trabajado en estas dos sesiones, para retomar desde cualquier agente.
> La guía operativa para diseñar/rediseñar pets es **`docs/GUIA_DISENO_PETS.md`** (leer primero).

## 1. QA integral del proyecto (commits `1e64e3e…8a0abfa`)

- **Resuelto y verificado el bug "mancha"** del cambio de skin en vivo (abierto desde el
  handoff del 06-30). Eran DOS bugs: (a) `FileView.blockLoading` no bloquea cambios de path
  en vivo → `blockAllReads: true`; (b) la property encadenada `manifestPath` se leía STALE
  dentro de `onActiveCharacterChanged` → `loadCharacter()` deriva rutas localmente.
  Además **Pet.qml monta UN solo backend vía Loader** (antes los 6 vivían ocultos:
  el VRM de Vivi cargado bajo cualquier skin, SpriteBackend animando 24/7).
- 4 revisores paralelos → ~35 hallazgos; corregidos los altos/medios: cerebro claude-cli
  que no arrancaba en instalación limpia, "Re-detectar" pisando ai.toml, idle-unload en
  modo agente, chat congelado en "Encendiendo…", watchdogs anti-atasco (chat/AutoVision),
  respawn de daemons de voz/agente, settings por stdin con 0600 (keys iban por argv),
  race del guardado, persona global que congelaba a todas las skins, pet muda sin error si
  faltaba voz Piper, lip-sync 1.5s adelantado, `/no_think` fuera, SSRF por redirect en
  tools_exec, instalador multi-distro (qt6-quick3d/grim/curl/ffmpeg, unidad con ruta real
  de quickshell, Piper por arquitectura, descargas atómicas, VRAM AMD por sysfs).
- Decisiones ejecutadas: `victoria.glb` (CC0 verificada) trackeado; `aka_live2d` fuera del
  índice (modelo no distribuible); `config/miiamia.toml` eliminada (muerta).
- Merge a `main` + push. Quedan ~8 hallazgos de severidad baja anotados en los reportes de
  los revisores (races de ms, locks menores) — no aplicados a propósito.

## 2. "Vida propia" (commits `2135199…5db7140`)

- **`shell/Reactions.qml`**: reacción POR EVENTO al contexto (juego/música/video/typing) —
  instantánea, local, sin cerebro. Frases `manifest.reactions.<estado>` o defaults.
  Anti-spam: settle 3.5s + cooldown 15min/estado + gap global 90s + gracia de boot.
  Verificada offscreen (matriz) y EN VIVO (mpv → watching → frase de Vivi con voz).
- **Voz temática en 25 skins** vía `manifest.voice.fx` + perfil `ghost` nuevo;
  auto-descarga de voces faltantes (voice_daemon `_ensure_voice`).
- **Ojos por voz**: "mira mi pantalla / qué ves" en el PTT → grim + turno multimodal
  (gate `vision` por provider; regex `_VISION_RE` en voice_daemon).
- Toggles "Vida propia" en el menú ⚙; globo con duración según texto; AutoVision con
  pista de contexto en el prompt.

## 3. Otto — Pulpo LEGO 3D (commit `6f3fd6b`)

- Pet 3D **procedural** (primitivas, cero assets externos) con etología real de pulpo:
  cromatóforos (color = humor, ondas passing-cloud), 8 brazos semi-autónomos por curva
  paramétrica, caricias por hover (suave=gusto, brusco=deimático+TINTA de piezas LEGO+
  huida a chorro que mueve la ventana), respiración por sifón, sueño en colores,
  camuflaje con el escritorio (grim+magick), modo guarida, baile.
- **Introduce el patrón de motor de humor** (arousal/pleasure → todo derivado) y las
  entradas de caricia en Pet.qml (petX/petY/petSpeed/held) + `jetEscape()`/`_dashAway`.
- Docs: `docs/OTTO_PULPO.md`. Voz fx `bubble`.

## 4. Nyx — humana cyberpunk hiperrealista (commits `01af9a1`, `015257a`)

- **La técnica de hiperrealismo del proyecto**: retrato RealVisXL V5 local + variantes por
  inpainting local (parches sin costura: parpadeo, miradas, visemas, sonrisa) + micro-
  etología humana + vida cyber (boot, glitch RGB, teletransporte, neón que late lub-dub
  con bloom). Docs: `docs/NYX_CYBER.md`; pipeline reproducible `tools/art/gen_nyx.py`.
- **Venv SDXL resucitado** (torchvision +cu128 forzado, peft) y generación CONVIVIENDO
  con un juego abierto (sequential offload + VAE en teselas forzadas con 1.6GB libres).
- Backend `CyberHumanBackend.qml`; voz fx `cyber`.

## 5. Sonido cinematográfico (commits `015257a`, `b789ed8`)

- Feedback del usuario: los synth crudos eran "muy de Atari" → **`tools/art/sfx_kit.py`**:
  motor DSP numpy+scipy (reverb Schroeder con cola, FM bell, pads desafinados, barridos de
  ruido overlap-add, granular, bitcrush-como-textura, sub saturado). REGLA: todo sonido
  nuevo = 2-4 capas + reverb; definir la acústica del MUNDO de la pet primero.
- Nyx (holo-tech) y Otto (submarino: burbujas Minnaert, paso-bajo de agua, reverb de
  tanque) regenerados y verificados por análisis espectral.

## 6. Estado

- **Git**: todo en `main` y pusheado a GitHub (`b789ed8` + docs). Rama feature = main.
- **Pet activa del usuario**: `nyx_cyber`. En el menú: 41 skins (Nyx, Otto, Vivi, Averno v3…).
- **Config runtime**: provider `claude-cli`, auto_vision on, reactions on, persona global
  liberada (cada pet usa la suya).
- **Pendientes sugeridos**: hallazgos de severidad baja del QA; remaster de sonidos de las
  skins kawaii si se quiere (sfx_kit listo); poses por estado del ajolote (venv ya funciona);
  gatillo de ojos por hotkey global.

## 7. Referencias rápidas

- Guía de diseño de pets (EL doc): `docs/GUIA_DISENO_PETS.md`
- Detalle por técnica: `DRAGON_RIGS.md` (2.5D) · `WAIFU_3D_PLAN.md` (VRM) ·
  `OTTO_PULPO.md` (3D procedural) · `NYX_CYBER.md` (hiperreal)
- Memoria del agente: `~/.claude/projects/<proyecto>/memory/MEMORY.md`
- Verificación: harness en scratchpad (Pet.qml+backends por symlink) + grim; ver §10 de la guía.
